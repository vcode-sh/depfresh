# depfresh 2.0.2: trustworthy legacy write receipts

depfresh 2.0.2 is a focused safety hotfix for normal `check --write` runs. It keeps the public
machine schemas and the existing 2.0.x package-by-package write model unchanged while making Git
preflight evidence and human receipts truthful on large or partially unavailable repositories.

## What changed

- exact-target Git evidence no longer inventories the complete tracked index;
- VCS preflight blocks retain VCS_UNAVAILABLE instead of WRITE_FAILED;
- repeated non-success writes are grouped by physical target;
- normal 2.0.x writes remain package-by-package until 2.1.0.

Exact Git failures remain attached to the canonical target they block, global-manager outcomes stay
outside local file counts, and the complete receipt is emitted as one ordered stdout block. Reverted
outcomes show recovery counts, while strict resolution and post-write failures retain the final
command exit instead of printing a contradictory success.

The local package verifier now applies fixed deadlines to its child processes and removes only the
temporary directories it owns. No published schema bytes, invocation-authority rules, manager
phases, artifact-trust boundaries, runtime requirement, or package-manager support changed.

## Upgrade

```bash
pnpm add -D --save-exact depfresh@2.0.2
pnpm exec depfresh --version
```

Node.js `24.15.0` or newer remains required.

## Local release-candidate evidence (not public)

This section records local candidate evidence only. It does not prove hosted `main`, npm
publication, a GitHub release, or public provenance.

The candidate was installed and verified on 2026-07-18 with exact Node `24.15.0`, pnpm `10.33.0`,
and isolated npm `11.12.0`. The frozen install used disposable home, cache, store, and npm config
paths. `pnpm-lock.yaml` remained byte-identical with SHA-256
`bf17552fd7c9e4a851d3b39762aea0741aef4232d042f7e34ef1aacc87ad2a2d`.

The local release-workflow replay produced these observed results:

- generated schemas, typecheck, Biome, and zero-warning Biome passed; Biome checked 309 files;
- the build passed with 1.87 MB of distribution output and nine schemas totaling 218 kB;
- all 102 release-focused tests passed;
- all 1,559 tests across 143 files passed with 87.06% statement, 80.07% branch, 93.76%
  function, and 89.52% line coverage;
- the practical CLI smoke passed 34 checks with 63 isolated registry requests;
- the WUN-shaped demo passed 14 checks across five packages and 12 occurrences, with three cold
  registry requests and zero additional warm requests.

Exact npm `11.12.0` produced the same identity in the dry run and final pack. The exact-manifest
verifier accepted every required distribution, schema, and skill asset, installed the tarball with
lifecycle scripts disabled, and observed CLI version `2.0.2`:

- Filename: `depfresh-2.0.2.tgz`
- Files: 56
- Packed size: 287,639 bytes
- Unpacked size: 1,891,768 bytes
- SHA-1: `5a1653832000f7aa85ea8fcf77bdd914bf9a4795`
- SHA-256: `51ce49b65fef3801aa9dd6efeba469f17760a9e71f4d0294d52339450ff4c5af`
- SHA-512 integrity:
  `sha512-MJu6Ma+xc2twvCQj8W7z6gkca1qFlw8v+m+Bzgmuh2bD0GTeexBNNIl0Fvbe3odGMQcDpY8F2FcW/Ha8LBiHUg==`

The built CLI and the exact installed tarball CLI separately passed the oversized-index hotfix
reproduction. Each disposable repository contained 18,003 tracked files and 1,368,035 bytes of
full `git ls-files -z` output, with one exact root target. Both runs planned and applied one update
with `APPLIED`, made two registry requests (`oversized-index-probe` and `pnpm`), and reported zero
failed resolutions, failed writes, or unknown writes. Neither output contained `ENOBUFS` or
`Write unknown (WRITE_FAILED)`. In both runs only `package.json` changed in the worktree, cached Git
diffs stayed empty, Git index bytes remained identical, and no optional lock residue remained.

## Release verification

The immutable `v2.0.2` tag workflow is configured to rebuild under exact Node `24.15.0`, install
isolated npm `11.12.0`, run schema, type, lint, coverage, smoke, demo, and installed-package gates,
publish only the verified tarball through npm trusted publishing, verify the public package, and
create the GitHub release from this file. It does not create or move a floating `v2` tag.

Before tag creation, the exact candidate must pass the complete local release workflow and the
hosted `main` workflow at the release commit. Public completion additionally requires npm, GitHub,
the retained workflow artifact, and provenance to agree on the same package bytes.

### Public release evidence

Release commit `45ac1d44cc50bbfaa87f74635308d63af4a9cab0` passed all four jobs in
[hosted main run 29631547257](https://github.com/vcode-sh/depfresh/actions/runs/29631547257).
Annotated tag object `7bfa63ba95c80a664f17119a12459e0c3561cd72` peels to that exact commit and
triggered [Release run 29631713748](https://github.com/vcode-sh/depfresh/actions/runs/29631713748),
whose artifact verification, trusted npm publication, public-package verification, and curated
GitHub release jobs all passed.

npm exposes `latest=2.0.2`. The retained Actions artifact `8425727523`, direct public-registry
download, exact npm `pack` download, and
[GitHub release asset](https://github.com/vcode-sh/depfresh/releases/tag/v2.0.2) are byte-identical.
The Actions artifact is retained until `2026-07-25T05:11:20Z`; npm and the non-draft,
non-prerelease GitHub release retain the public package independently. The exact public values are:

- Filename: `depfresh-2.0.2.tgz`
- Files: 56
- Packed size: 287,639 bytes
- Unpacked size: 1,891,768 bytes
- SHA-1: `5a1653832000f7aa85ea8fcf77bdd914bf9a4795`
- SHA-256: `51ce49b65fef3801aa9dd6efeba469f17760a9e71f4d0294d52339450ff4c5af`
- SHA-512 integrity:
  `sha512-MJu6Ma+xc2twvCQj8W7z6gkca1qFlw8v+m+Bzgmuh2bD0GTeexBNNIl0Fvbe3odGMQcDpY8F2FcW/Ha8LBiHUg==`

Exact npm `11.12.0` installed and verified the public package with lifecycle scripts disabled. The
installed CLI reported `2.0.2`; all required schemas, exports, and packaged skill assets resolved.
`npm audit signatures --json --include-attestations --ignore-scripts` returned empty `invalid` and
`missing` sets and independently verified the registry signature plus the publish and SLSA
attestation bundles for `depfresh@2.0.2`. The package signature uses npm key
`SHA256:DhQ8wR5APBvFHLF/+Tc+AYvPOdTpcIDqOhxsBHRwC7U`; the publish and provenance transparency-log
indexes are `2193500102` and `2193500094`.

The public installed CLI also passed the oversized-index hotfix reproduction in a disposable Git
repository with 18,003 tracked files and 1,368,031 bytes of full `git ls-files -z` output. It made
the exact `oversized-index-probe` and `pnpm` registry requests, applied the one planned manifest
update with `APPLIED`, and emitted neither `ENOBUFS` nor `WRITE_FAILED`. Only `package.json`
changed, the cached diff remained empty, Git index SHA-256 remained
`8719b278ae3d9590f2e2e8408724fa666447a17f34671293b576e8e7bd53e8a9`, and no apply-state
residue remained.

The verified public SLSA v1 statement binds `pkg:npm/depfresh@2.0.2` and SHA-512 digest
`309bba31afb1736b70bc2423f16ef3ea091c6b5a85970f2ffa6f81ce09ae8766c3d064de7b104d34897416f6dede8746310703a58f05d85716fc76bc2c188752`
to `refs/tags/v2.0.2`, `.github/workflows/release.yml`, release commit
`45ac1d44cc50bbfaa87f74635308d63af4a9cab0`, run `29631713748` attempt 1, and the
GitHub-hosted builder. The statement carries SHA-512 only; SHA-1 and SHA-256 agreement is proven
separately by byte comparison. The annotated Git tag is unsigned, so this release makes no signed
Git-tag claim. No floating `v2` tag or manual publish was created.
