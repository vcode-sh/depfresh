# depfresh 2.1.1: local hybrid Visual+ release candidate

Status: retained local release-candidate evidence for the Plan 038 hybrid human default.

No npm publication, Git tag, GitHub release, hosted workflow, or public artifact is claimed.

## Plan boundary

- Plan 037 completed the compact semantic contract for the then-current default projection.
- Plan 038 owns the hybrid human default and has independent replacement evidence pending final
  evidence review.
- `--long` preserves complete operation, owner, shared-dependency, occurrence, and physical-target membership.

Interactive, JSON, global, silent, veto-capable, and public library behavior remains unchanged.
The hybrid default has a five-region review and a complete ledger; successful output has no durable
lifecycle rail. See the [hybrid design](../superpowers/specs/2026-07-20-visual-plus-hybrid-default-design.md)
and [Plan 038](../../plans/038-visual-plus-hybrid-default.md).

The retained Plan 038 artifact, installed replay, Bun replacement, and live Spreadoo proof below
supersede the Plan 037 local candidate for current use. The older records remain historical and
provide the exact rollback artifact used before the scoped Bun replacement.

## Reviewed Plan 038 source proof

Task 4 is independently approved C0/I0/M0 over `a6dd53d..6e9f59c` (`57327c9`, `34dfdaa`,
`50391f2`, and `6e9f59c`). The deterministic Visual+ contract is exactly 1 file, 5 suites, and 69
passed tests with zero failed, pending, todo, or retry results. All five hybrid journey titles
classify as `visual-hierarchy`; retired compact titles remain unclassified. Do not combine that
exact 69-test contract with the surrounding gates: the focused proof gate passed 4 files and 92
tests, while the source-coupled gate passed 5 files and 161 tests including those 69 Visual+ tests.
Build, typecheck, Biome, syntax, formatting, and diff checks passed during the reviewed task.

The reviewed local proof infrastructure binds contained no-replace evidence, complete default and
`--long` membership, Bun and `bunx` dispatch identity, and unchanged repository identities. Its
support helper remains unshipped.

## Retained Plan 038 local release candidate

The exact package source commit is `70c4fcff728e4197362d86f286f451700fc4e11b`. Release work used
Node `24.15.0`, isolated npm `12.0.1`, pnpm `11.15.1`, and Bun `1.3.14`. The current-stable source
gate passed schema checks, typecheck, zero-warning Biome, build, full coverage, release, smoke,
demo, and package verification. The retry-disabled full run passed `168/168` files and
`2422/2422` tests. Coverage was statements `88.68%`, branches `82.18%`, functions `95.03%`, and
lines `90.91%`. The direct-Node live-harness correction additionally passed its focused
`2`-file/`67`-test PTY identity gate.

One isolated artifact is retained at
`/private/tmp/depfresh-2.1.1-hybrid.9WwdE9/depfresh-2.1.1.tgz`:

- manifest files: `87`;
- packed bytes: `282910`; unpacked bytes: `1531841`;
- SHA-1: `4008779ff58ac75727f68a3d5d449397c79cd8a4`;
- SHA-256: `145fa43da00b9f95c892863be937f88dc637e6549760469710a7f943be9371df`;
- SHA-512 integrity:
  `sha512-EE58S93hPj+D7DiR+hHdS1jvFwZiwn/Eqqx8gKu37t1+2CBTgvWwZ1XfMnkiqKdwN+vo3ySGTAZ5wE6KX5BCKg==`;
- installed CLI SHA-256:
  `4e41339ce1e7d6818602eeced1d0a7d4a5ef63374f593e5a351b491e3aff87a7`;
- artifact-bound installed Visual+ replay: `1` file, `5` suites, and `69/69` passed tests with
  zero failed, pending, todo, or retry results.

Bun removed only the prior depfresh candidate and installed that exact retained tarball with
lifecycle scripts disabled. Both `bunx --no-install depfresh --version` and
`bunx depfresh --version` printed `2.1.1`; the global CLI SHA-256 matches the installed replay.
The unrelated global packages `@tanstack/cli@0.62.3`, `ccusage@18.0.5`, `clawdhub@0.3.0`, and
`opencode-ai@1.14.19` remained present.

The fixed-argv live harness ran the global installed candidate against Spreadoo at `80` and `118`
columns in both default and `--long` modes. Each run exited `0` with a visible cursor. The default
journey rendered the approved five-region hierarchy and exactly `99/99` update rows across `21`
physical files: `6` major, `47` minor, and `46` patch. The exhaustive journey retained `99`
operations, `22` owners, `22` shared dependencies, `55` occurrences, and `21` targets.

Spreadoo remained byte-for-byte unchanged: HEAD
`93bbe6a4b3f4c932d8a445c9cad7c15213dd1db6`, index SHA-256
`8ac399923982959850218f2a87231f8de54b3d60f0fbcd08015038d126715df4`, and `bun.lock` SHA-256
`69427f40e5a93a13ac2bd4597f52314f4a731c21fc760684b69ea372f87e2dcb` matched before and after;
status, working diff, and cached diff remained empty. The machine evidence is retained at
`/private/tmp/depfresh-2.1.1-hybrid.9WwdE9/installed-replay.json` and
`/private/tmp/depfresh-2.1.1-hybrid.9WwdE9/spreadoo-live.json`.

This is local release-candidate evidence only. It does not claim npm publication, a Git tag,
GitHub release, hosted workflow, public artifact, push, or production deployment.

## Retained Plan 037 evidence

## Corrected source gate

The final-review corrections are committed in `d72140d`; evidence documentation was current at
`a0efe4d`. Exact Node `24.15.0`, npm `11.12.1`, and pnpm `10.33.0` were used for every release
command.

- Schema check, typecheck, full zero-warning Biome across `358` files, and build: passed.
- Full coverage: `164/164` files and `2262/2262` tests passed without retry.
- Coverage: statements `88.48%`, branches `81.96%`, functions `94.89%`, lines `90.70%`.
- Release suite: `5/5` files and `106/106` tests passed.
- Local package verification: `56` files, `336301` packed bytes, `2129683` unpacked bytes.

## Corrected retained artifact

One artifact was packed from corrected HEAD with lifecycle scripts disabled and isolated home,
cache, npm configuration, prefix, pnpm-home, XDG cache, and temporary directories. The verifier
installed these exact bytes into a disposable consumer and replayed the installed Visual+ suite.

- Artifact path:
  `/private/tmp/depfresh-2.1.1-corrected.8P8wvp/depfresh-2.1.1.tgz`
- Filename: `depfresh-2.1.1.tgz`
- Files: `56`
- Packed bytes: `336301`
- Unpacked bytes: `2129683`
- SHA-1: `07e0f30c206356033597b29b18d8874aa69beff3`
- SHA-256: `fcd864a34cd45c7fa0c0f379efea7048c17ca0c51aa0843ae91786bda5ba71f1`
- SHA-512 integrity:
  `sha512-RaTDw2OKIKQ3tIAIs9P0dR1mlblL02sSGw4OKd1FWheVhImS4RAb9c9Z/Ocs7XiCeH6xelwRcreenIprZLDEtg==`
- Installed CLI SHA-256:
  `1020cf266ed6011a432961047e3e64c938ac90e3d6079d63443f6d83136e2958`
- Installed Visual+ replay: `58/58` tests and assertions, `5/5` suites, zero failed,
  pending, or todo results, and zero retries.

This is exact local candidate evidence only. It does not establish registry availability,
publication, a tag, a hosted workflow, a GitHub release, or public-artifact identity.

## Corrected Bun install and live repository smoke

Bun `1.3.14` removed only the superseded global depfresh entry and installed the exact corrected
tarball with lifecycle scripts disabled. Other global packages were retained. The bin symlink
resolves to the global package CLI, whose SHA-256 matches the verified installed CLI above. Both
required probes printed `2.1.1`:

```text
bunx --no-install depfresh --version  -> 2.1.1
bunx depfresh --version               -> 2.1.1
```

A true BSD PTY ran the literal read-only `bunx depfresh major` journey in the current Spreadoo
checkout at 118 columns with pinned Node available to the package shebang. The projected compact
transcript contained `63` durable lines, truthful `spreadu` and Bun `1.3.14` context from
`bun.lock` and `package.json`, the `--long` hint, no internal operation, owner, dependency,
package, or source IDs, a visible final cursor, and final `Exit 0`.

The successful run retained exact pre/post state:

- HEAD: `8f88b34d9361e17aba81e7086e0636b7837f63b7`;
- Git index SHA-256: `d5508f5610172aded9e250134459aaef2cfdc4961dd424b0d42753af74c112ba`;
- `bun.lock` SHA-256: `69427f40e5a93a13ac2bd4597f52314f4a731c21fc760684b69ea372f87e2dcb`;
- status, working-tree diff, and cached-diff SHA-256:
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.

## Superseded local artifact evidence from `41f0002`

The earlier artifact remains retained for audit and rollback only. Its SHA-256 was
`5c38c97902abd625533ce36fdff9e889c7d719a4baffd1647ae00ebad1b98636`, with `335933` packed and
`2128604` unpacked bytes. It does not describe the corrected candidate above.

The interim correction record said `Status: corrected local source candidate; artifact regeneration pending.`
At that point, none of the historical evidence below describes the corrected HEAD. That source-only
correction did not rebuild, repack, reinstall, publish, tag, push, run hosted workflows, or repeat the live
Spreadoo smoke. The corrected artifact refresh has now replaced only those pending local evidence
steps; the public and hosted boundaries remain unchanged.
