# depfresh 2.1.1: compact Visual+ candidate

Status: local candidate only.

No npm publication, Git tag, GitHub release, hosted workflow, or public artifact is claimed.

## Highlights

- Eligible local CLI table journeys default to compact Visual+ output.
- Successful compact acceptance journeys stay within 80 durable projected lines.
- Compact output hides internal IDs while retaining every major-risk card and non-success target or recovery fact.
- `--long` preserves complete operation, owner, shared-dependency, occurrence, and physical-target membership.
- Repository and package-manager context comes from post-discovery evidence instead of startup placeholders.

Interactive, JSON, global, silent, veto-capable, and public library behavior remains unchanged.
Ordinary read-only `not-attempted` target previews may be bounded. Every non-success target in
write safety or recovery bypasses compact limits, and recovery evidence remains visible.

## Verification scope

The release candidate is verified locally with the pinned Node `24.15.0`, npm `11.12.1`, and pnpm
`10.33.0` toolchain. Local artifact evidence is recorded only after one isolated tarball is packed
and replayed from its exact bytes.

## Exact local artifact evidence

One retained artifact was packed with lifecycle scripts disabled in disposable home, cache,
configuration, prefix, store, and temporary directories. The verifier installed that exact local
tarball into a disposable consumer and replayed the Visual+ suite without retries.

- Artifact path:
  `/private/tmp/depfresh-2.1.1-candidate.TSKdAy/depfresh-2.1.1.tgz`
- Filename: `depfresh-2.1.1.tgz`
- Files: `56`
- Packed bytes: `335933`
- Unpacked bytes: `2128604`
- SHA-1: `c33baaf7e541c2b88ca05b9588f64f5af2ca02ca`
- SHA-256: `5c38c97902abd625533ce36fdff9e889c7d719a4baffd1647ae00ebad1b98636`
- SHA-512 integrity:
  `sha512-AZqqEqNMyXYLMt91mII9/z0XCyxa5sAz6Q1zvDZLh4p+CdtxkAs3HzcuqVxYUAg+Y9cWNl4Mq/iOQsEqNpPaSw==`
- Installed CLI SHA-256:
  `1020cf266ed6011a432961047e3e64c938ac90e3d6079d63443f6d83136e2958`
- Installed Visual+ replay: `58` passed tests, `0` failed tests, `0` retries.

This is exact local candidate evidence only. It does not establish registry availability,
publication, tag, hosted workflow, GitHub release, or public-artifact identity.

## Local source and package gates

The corrected fresh gate passed with Node `24.15.0`, npm `11.12.1`, pnpm `10.33.0`, and zero test
retries. The built CLI was refreshed before any suite that resolves `dist/cli.mjs`.

- Full coverage: `164` files and `2256` tests passed; statements `88.43%`, lines `90.65%`.
- Practical CLI smoke: `36` checks passed with `680` fixture-registry requests.
- WUN-shaped demo: `14` checks passed across `5` packages and `12` occurrences.
- Release suite: `5` files and `106` tests passed.
- Local package verification: `56` files installed from exact tarball bytes.

## Bun global install and live repository smoke

Bun `1.3.14` installed the exact retained tarball globally with lifecycle scripts disabled. The
global CLI SHA-256 matches the installed CLI hash above. Both required resolution paths printed
`2.1.1`:

```text
bunx --no-install depfresh --version  -> 2.1.1
bunx depfresh --version               -> 2.1.1
```

A true-PTY, read-only `bunx depfresh major` smoke ran in the current Spreadoo checkout. Live
repository evidence identifies `spreadu` and the observed manager as Bun `1.3.14`, sourced from
`bun.lock` and `package.json`. The compact projection contained `74` durable lines, no internal
operation, owner, dependency, package, or source IDs, and ended with `Exit 0`.

Spreadoo remained byte-clean: HEAD stayed at `8f88b34d9361e17aba81e7086e0636b7837f63b7`,
the Git index SHA-256 stayed
`2706233d114adb4bc833888938fd20144eb5b8f146e0fd7cb5a96a30e000bfd0`, and status, working-tree
diff, and cached-diff SHA-256 values remained the empty-input digest
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.
