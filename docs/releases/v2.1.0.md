# depfresh 2.1.0: command-level safety and Visual+ journeys

depfresh 2.1.0 is the minor release for the reviewed command-level local apply lifecycle and
Visual+ terminal results. It keeps machine schemas, public library contracts, and explicit
invocation authority stable while making local write receipts and terminal fallback behavior more
useful and more honest.

## What changed

### Command-level preflight

- Command-level preflight covers every selected physical target before the first replacement.
- One root-local lock and journal lifecycle covers the prepared local command, while unrelated dirty
  targets remain outside the selected write scope.

### Visual+ hierarchy

- Visual+ hierarchy keeps topology, severity, major risk, owner impact, shared dependencies, and final receipts legible.
- Complete, safety-block, and recovery-first journeys retain exact operation counts, physical paths,
  lifecycle evidence, and conservative next actions before their final exit status.

### Complete fallbacks

- Complete fallbacks preserve append-only output in pipes, CI, and TERM=dumb without terminal control leakage.
- NO_COLOR and narrow capable terminals retain the eligible plain-terminal journey without changing
  JSON, interactive, global, veto-capable, or public library contracts.

### Atomicity and recovery

- Replacement is atomic per file only; a repository is not an atomic transaction, and recovery remains best effort.
- Incomplete or unobservable recovery remains unknown and exits non-successfully.

## Upgrade

```bash
pnpm add -D --save-exact depfresh@2.1.0
pnpm exec depfresh --version
```

Node.js `24.15.0` or newer remains required.

## Release verification

The immutable `v2.1.0` tag workflow published the exact reviewed candidate after its complete local
source and packed-artifact gates and the hosted `main` workflow passed. The published npm package,
retained workflow artifact, GitHub release, public installed-product replay, signatures, and
provenance all agree on the same bytes.

### Local candidate evidence

The exact candidate at commit `1b2fca3024f931d1f212dbf2f69ed7011ef22b74` passed independent
macOS arm64 and Linux arm64 lanes under Node `24.15.0`, npm `11.12.1`, and pnpm `10.33.0`.
Both lanes used disposable HOME, cache, store, temporary, install, and artifact paths; frozen
installation left the candidate and `pnpm-lock.yaml` unchanged.

Each operating system passed schemas, typecheck, lint, zero-warning Biome, build, all 161 test files
and 2,140 tests with coverage, practical smoke, the WUN-shaped demo, 103 release tests, the 10-test
command transaction fault matrix, 32 source Visual+ tests, and 62 pure capability/renderer tests.
The retained Visual+ matrix covered success and safety-block journeys at 40, 60, 80, and 118
columns plus non-TTY, slow-pipe, `NO_COLOR`, CI, and `TERM=dumb` fallbacks. The supported Linux lane
ran non-root with a reaping init; focused one-variable controls proved why the discarded root and
non-reaping harness could not supply valid permission or descendant-cleanup evidence.

Both platforms produced the same exact artifact:

- Filename: `depfresh-2.1.0.tgz`
- File count: `56`
- Packed size: `332890` bytes
- Unpacked size: `2113827` bytes
- SHA-1: `d006c27a0f91ae6f458e6ba324efcac1bac566a4`
- SHA-256: `5d17e2a43a1c76160f0b95214b956a0d100a2e7e0bcfc2eb0b0c4c6f8143c833`
- SHA-512 integrity:
  `sha512-OkDqQ4V1MKAeJJbMBbXYTiigjFqV72aD4UEAHuHSKM0sc/G1j4kQZCRqTnPXFY792LGhzBaLbg2sJ3ZPOn4Rbg==`
- Installed CLI SHA-256:
  `3a7980e4be50ff11e732ac1c9e47c1e4b6583abf573d036b6326fc5ab6dcbdfd`
- Installed-artifact Visual+ result: `32` tests passed on each platform

Current retained Visual+ verifier contract: `54` tests after the diagnostic-only fallback splits,
the bounded privacy-preserving raw CI transport classifier, the BSD outer-PTY transport regression,
the title-only CI and TERM=dumb fallback phase splits, the fail-closed process-identity lifecycle
regressions, and the private child-write and inner-output-mode boundary diagnostics. The published
artifact evidence below remains the immutable 50-test release replay.

### Public release evidence

Release commit `8c4b9dd479e672f0c937946406603c0988f36e37` passed every job in
[hosted main run 29699269620](https://github.com/vcode-sh/depfresh/actions/runs/29699269620).
Annotated tag object `3f8c74b3502ee6fe55abddddcb5d03d3df26800f` peels to that exact commit
and triggered
[Release run 29699466746](https://github.com/vcode-sh/depfresh/actions/runs/29699466746).
Artifact verification job `88225931219`, trusted publication and public-install job `88226561867`,
and curated release job `88226641405` all passed on attempt 1. No floating `v2` tag was created, and
no manual publication occurred.

npm exposes `latest=2.1.0`. The tarball inside retained Actions artifact `8446072086`, the direct
registry download, exact npm `pack` download, and the
[GitHub release asset](https://github.com/vcode-sh/depfresh/releases/tag/v2.1.0) are byte-identical.
The Actions artifact is retained until `2026-07-26T18:58:51Z`; GitHub asset `482646293` is public in
a non-draft, non-prerelease release. All four copies retain the exact local candidate identity
recorded above, including 56 files, 332890 packed bytes, 2113827 unpacked bytes, and CLI SHA-256
`3a7980e4be50ff11e732ac1c9e47c1e4b6583abf573d036b6326fc5ab6dcbdfd`.

Exact Node `24.15.0` and npm `11.12.1` installed `depfresh@2.1.0` from the public registry with
lifecycle scripts disabled. The installed CLI reported `2.1.0`; the schema-v2 capabilities contract,
all seven commands, nine contract schemas, eighteen package exports, and eleven capability assets
resolved. The installed manifest retained exactly the 17 declared runtime dependencies, kept
`node:sqlite`, and did not introduce `better-sqlite3`.

The public installed artifact passed the complete Visual+ replay 50/50 without retry. That replay
retained the deterministic 616-declaration, 76-update, 14-target fixture, the 18/39 shared surface,
both major-risk cards, exact receipts and exits, terminal-control containment, and final filesystem
state across the success, safety-block, capable PTY, constrained, and pipe journeys.

`npm audit signatures --json --include-attestations --ignore-scripts` returned empty `invalid` and
`missing` sets while verifying 30 registry signatures and nine attestation-bearing packages. The
depfresh signature uses npm key
`SHA256:DhQ8wR5APBvFHLF/+Tc+AYvPOdTpcIDqOhxsBHRwC7U`; its publish and provenance transparency-log
indexes are `2203237229` and `2203236939`.

The verified SLSA v1 statement binds `pkg:npm/depfresh@2.1.0` and SHA-512 digest
`3a40ea43857530a01e2496cc05b5d84e28a08c5a95ef6683e141001ee1d228cd2c73f1b58f891064246a4e73d7158efdd8b1a1cc168b6e0dac27764f3a7e116e`
to `refs/tags/v2.1.0`, `.github/workflows/release.yml`, release commit
`8c4b9dd479e672f0c937946406603c0988f36e37`, run `29699466746` attempt 1, and the GitHub-hosted
builder. The annotated Git tag is unsigned, so this release makes no signed-tag claim.
