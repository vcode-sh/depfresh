# depfresh 2.1.0 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish the reviewed command-level apply and Visual+ v2 product as `depfresh@2.1.0` and
prove the exact public artifact and terminal journeys.

**Architecture:** Prepare one version-coupled minor-release candidate after Plans 032-035 pass,
replay source and packed CLI gates including real PTY journeys, require hosted `main` success,
then publish only through the immutable `v2.1.0` OIDC tag workflow. Public proof reruns success
and safety journeys against installed npm bytes.

**Tech Stack:** TypeScript, Node `24.15.0`, pnpm `10.33.0`, npm `11.12.x`, Vitest, Biome, GitHub
Actions, real PTY tests, npm trusted publishing, GitHub Releases.

## Global Constraints

- All code, documentation, plans, and commit messages are English.
- Begin only after Plans 032-035 are complete with independent approvals.
- Preserve all historical release notes, published schema bytes, and immutable release tags.
- Keep Node `>=24.15.0`, pnpm `10.33.0`, ESM-only output, no new runtime/native dependency, and the
  approved Visual+ fallback matrix.
- Use disposable HOME, cache, store, install, repository, PTY, and artifact paths for every proof.
- Do not publish locally, create a floating `v2` tag, force-move `v2.1.0`, bypass hosted OIDC, or
  claim public completion from local/hosted evidence alone.
- Stop on any missing row/target, terminal leak, filesystem mismatch, schema drift, failed recovery
  assertion, ambiguous artifact identity, or public provenance mismatch.

## Drift Check and Stop Conditions

Before editing, run `git status --short`, verify Plans 032-035 are complete with recorded review and
PTY evidence, and recheck live npm/GitHub absence for `2.1.0`/`v2.1.0`. Stop if any prerequisite is
incomplete, release surfaces overlap unrelated work, local/hosted/public proofs disagree, a required
PTY gate is skipped, publication would require manual npm access or tag movement, or provenance
cannot bind the exact artifact to the exact release commit.

---

### Task 1: Version-coupled minor release surfaces

**Files:**

- Modify: `package.json`
- Modify: `README.md`
- Modify: `CHANGELOG.md`
- Modify: `.github/workflows/release.yml`
- Modify: `.github/ISSUE_TEMPLATE/bug_report.yml`
- Modify: `docs/README.md`
- Modify: `docs/agents/README.md`
- Modify: `docs/integrations/README.md`
- Modify: `skills/depfresh/recipes/runners.md`
- Modify: `test/wun-demo-proof.mjs`
- Modify: `test/release-readiness.test.ts`
- Modify: `test/package-assets.test.ts`
- Create: `docs/releases/v2.1.0.md`

**Interfaces:**

- Consumes: completed Plans 032-035 at version `2.0.2`.
- Produces: internally consistent `2.1.0` candidate and release note.

- [x] **Step 1: Write release-readiness RED assertions**

Require version `2.1.0`, maintained runner pins, dated changelog, release note, workflow body path,
and a release note section for command preflight, Visual+ hierarchy, complete fallbacks, and exact
atomicity/recovery language.

- [x] **Step 2: Run RED release tests**

Run: `pnpm exec vitest run test/release-readiness.test.ts test/package-assets.test.ts`

Expected: FAIL on `2.0.2` current-version surfaces.

- [x] **Step 3: Update only current release surfaces**

Bump maintained references to `2.1.0`, move completed Unreleased entries to a dated `2.1.0`
section, and create the release note. Do not rewrite `2.0.x` incident/public evidence.

- [x] **Step 4: Run static release gates**

Run release tests, schemas check, typecheck, and lint. Expected: all exit `0`, no warnings.

**Task 1 evidence (2026-07-19):** Commit `ca3fc4f` prepared only the version-coupled `2.1.0`
release surfaces. The focused RED run failed with 5 expected failures and 24 passes on the retained
`2.0.2` values; the GREEN run passed all 29 release/package tests plus schema, typecheck, lint, and
diff checks under Node `24.15.0`, npm `11.12.1`, and pnpm `10.33.0`. Review commit `e99f16e`
also rejects a stale maintained `DEPFRESH_VERSION=2.0.2` pin. Independent task review reported
spec compliance and code quality approved with C0/I0/M0. This is a local candidate only; no push,
tag, publication, GitHub Release, or public-proof claim occurred.

### Task 2A: Retained installed-artifact Visual+ replay

**Files:**

- Create: `test/helpers/visual-plus-artifact-path.mjs`
- Create: `test/visual-plus-artifact-path.test.ts`
- Modify: `test/visual-plus-cli.test.ts`
- Modify: `scripts/verify-packed-package.mjs`
- Create: `scripts/visual-plus-replay-failure.mjs`
- Modify: `test/verify-local-package.test.ts`
- Create: `test/visual-plus-replay-failure.test.ts`
- Modify: `.github/workflows/ci.yml`
- Modify: `.github/workflows/release.yml`
- Modify: `test/release-readiness.test.ts`

**Interfaces:**

- Consumes: the reviewed Task 1 candidate and the Task 2 blocker review.
- Produces: a retained `--visual-plus` packed-verifier mode whose evidence names the canonical
  installed CLI path, installed/tarball CLI SHA-256, and exact passed Visual+ test count.

- [x] **Step 1: Write path-binding and workflow RED tests**

Add tests that require paired absolute CLI/install-root inputs, reject symlinks and paths outside
the canonical disposable installation, preserve the ordinary source-tree default, require the
packed verifier mode in both hosted platform gates, and reject a stale source-only workflow.

- [x] **Step 2: Prove the RED failures are causal**

Run the focused path, verifier, and readiness tests under Node `24.15.0`. Also run the existing
Visual+ suite with a deliberately invalid artifact override and prove it still passes because the
override is currently ignored. Expected: the new assertions fail only on the missing retained seam.

- [x] **Step 3: Implement the minimal retained replay seam**

Resolve one paired test-only CLI/install-root override at module load, require a non-symlink regular
CLI file contained by the canonical installation root, and retain repository `dist/cli.mjs` when no
override is present. Add a fixed, bounded `--visual-plus` verifier mode that, before cleanup:

1. extracts `package/dist/cli.mjs` from the exact verified tarball without a shell;
2. requires its SHA-256 to equal the installed CLI SHA-256;
3. runs a focused distinct-byte negative control that must fail;
4. runs the unchanged full Visual+ suite against the installed CLI with retries disabled; and
5. emits only sanitized aggregate evidence, including canonical path, hash, and passed test count,
   or one fixed bounded failure category from a size- and identity-bounded private report.

- [x] **Step 4: Wire both hosted operating-system gates**

Keep the source-tree Visual+ run, then pack once per `ubuntu-24.04`/`macos-15` matrix leg and invoke
the installed-artifact verifier mode. Require the release verification workflow to use the same mode
before upload while leaving publish retry output bounded and publication authority unchanged.

- [x] **Step 5: Run GREEN and independent review**

Run focused tests, build, source Visual+, local packed Visual+, schemas, typecheck, zero-warning
Biome, release-readiness, and diff checks with the exact toolchain. Require an independent spec and
quality review at C0/I0/M0 before marking Task 2A complete.

**Task 2A evidence (2026-07-19):** Commits `6957ebf` and `c30eec3` added the retained
installed-artifact replay and closed all three review findings. The causal RED proved the former
suite ignored an invalid artifact override. Fresh source and packed GREEN runs each passed 32/32
Visual+ tests; the packed CLI SHA-256 was
`3a7980e4be50ff11e732ac1c9e47c1e4b6583abf573d036b6326fc5ab6dcbdfd`. The verifier binds the
canonical installed path and extracted tarball bytes, rejects a distinct-byte outside-root negative
control, uses isolated disposable paths and fixed deadlines, and keeps raw child output private.
Both hosted OS legs pin isolated npm `11.12.0`. Build, schemas, typecheck, release tests,
zero-warning Biome, and diff checks passed. Independent re-review reported Spec Pass, Quality Pass,
and C0/I0/M0. No push, tag, publication, or public claim occurred.

### Task 2: Exact local source and packed PTY proof

**Files:**

- Verify: source, `dist/`, schemas, package manifest, skill assets, tarball, PTY fixtures
- Modify: `docs/releases/v2.1.0.md` with observed local artifact evidence

**Interfaces:**

- Consumes: Task 1 candidate.
- Produces: exact local artifact identity eligible for hosted publication.

- [x] **Step 1: Install exact isolated toolchain**

Use Node `24.15.0`, pnpm `10.33.0`, npm `11.12.x`, and disposable paths. Expected: frozen install
leaves repository and lockfile unchanged.

- [x] **Step 2: Run complete source gates**

Run schemas, typecheck, lint, full coverage, build, smoke, demo, release, command transaction fault
matrix, and every Visual+ PTY/fallback journey. Expected: all pass on macOS and Linux gates.

- [x] **Step 3: Pack, install, and rerun product journeys**

Verify the tarball, install it into a disposable prefix, then run the 76-row/14-target success and
safety-block journeys against installed bytes at 40/60/80/118 columns plus non-TTY/CI/dumb/colorless
modes. Expected: exact rows/targets/receipts, exit `0`/`2`, and filesystem hashes.

- [x] **Step 4: Record exact artifact identity**

Capture filename, file count, packed/unpacked sizes, SHA-1, SHA-256, SHA-512 integrity, and PTY test
counts. Rerun readiness after editing the release note.

**Stop condition invoked on 2026-07-19:** the retained Visual+ product-journey test binds every
CLI run to repository `dist/cli.mjs`. The packed verifier installs the exact tarball but does not
replay the 76-row/14-target PTY and fallback matrix against that installed CLI. Task 2 authorizes
only release-note changes, so the missing retained harness cannot be added inside this evidence-only
task. No Task 2 step is complete, no artifact identity was recorded, and Tasks 3-4 remain blocked.

Task 2A passed independent review; rerun this task from exact candidate commit `c30eec3`.

**Task 2 evidence (2026-07-19):** Exact candidate `1b2fca3` passed independent macOS arm64 and
corrected non-root, reaping-init Linux arm64 lanes under Node `24.15.0`, npm `11.12.1`, and pnpm
`10.33.0`. Each lane passed all 161 files and 2,140 tests with coverage, smoke, demo, 103 release
tests, the 10-test transaction matrix, 32 source Visual+ tests, 62 pure Visual+ tests, and 32
installed-artifact Visual+ tests. One-variable Linux controls independently proved that UID 0
invalidated permission fixtures and a non-reaping PID 1 invalidated descendant observation; the
fresh supported lane passed completely. Both platforms produced byte-identical
`depfresh-2.1.0.tgz`: 56 files, 332890 packed bytes, 2113827 unpacked bytes, SHA-1
`d006c27a0f91ae6f458e6ba324efcac1bac566a4`, SHA-256
`5d17e2a43a1c76160f0b95214b956a0d100a2e7e0bcfc2eb0b0c4c6f8143c833`, and SHA-512 integrity
`sha512-OkDqQ4V1MKAeJJbMBbXYTiigjFqV72aD4UEAHuHSKM0sc/G1j4kQZCRqTnPXFY792LGhzBaLbg2sJ3ZPOn4Rbg==`.
Installed CLI SHA-256 was
`3a7980e4be50ff11e732ac1c9e47c1e4b6583abf573d036b6326fc5ab6dcbdfd`. Final source and lock
hashes were unchanged, cleanup completed, and independent proof review passed at C0/I0/M0. This is
local evidence only; no hosted, tag, publication, or public-provenance claim occurred.

**Hosted stop and proof hardening (2026-07-19):** The first exact `main` run
[`29683662980`](https://github.com/vcode-sh/depfresh/actions/runs/29683662980) passed every job
except the macOS installed-artifact replay at exact SHA
`eed33777ac374b8f7e46a4ac585971e955c5457d`. Attempts 2 and 3 passed on the same SHA and runner
image, while attempt 2's source lane visibly required Vitest retries in PTY/process-observation
tests. Because the JSON reporter does not retain retry counts, those green attempts were not
accepted as stable release proof. Commits `deeaaf4` and `7d0fc14` now disable retries in both hosted
source and installed replay, classify only exact allowlisted test titles into fixed labels, and
bound the private report to a regular non-symlink file of at most 256 KiB with opened-descriptor
identity and exact-length checks. Missing, unsafe, unstable, oversized, malformed, or unknown
evidence becomes `unclassified`; raw child output, paths, messages, environment, and stacks remain
private. Exact-Node focused tests passed 34/34, source Visual+ passed 32/32 without retries, two
hosted-like packed replays each passed 32/32 without retries, and schemas, typecheck, release
readiness, zero-warning Biome, and diff checks passed. Independent re-review closed at C0/I0/M0.
The hardened candidate was pushed at exact SHA
`f9f1bc04eb3c539120dc7e3fbe8f1050973f4f17`. Run
[`29685151490`](https://github.com/vcode-sh/depfresh/actions/runs/29685151490) passed source Visual+
without retries on both platforms, Ubuntu installed Visual+, full tests, lint, build, and
distribution smoke, but the no-retry macOS installed replay stopped with the fixed `fallback`
classification. Commit `f4c6611` preserves every fallback assertion while splitting that combined
test into independent `fallback-direct`, `fallback-capable-pty`, and `fallback-constrained-pty`
contracts. The retained verifier now requires 34 tests; exact-Node focused tests passed 37/37 and
isolated source plus installed-artifact replays each passed 34/34 without retries. Independent
review reported C0/I0/M0. This diagnostic candidate remains local pending a new exact hosted run;
no tag or publication occurred.

The next exact `main` run
[`29685720822`](https://github.com/vcode-sh/depfresh/actions/runs/29685720822) at
`158ed25f944197053b53fd57d038622c0f2498c0` passed Ubuntu source and installed Visual+, full tests,
lint, build, and distribution smoke, but the no-retry macOS source lane exposed the isolated CI
fallback assertion: expected zero lone carriage returns and observed one. Repeated idle and bounded
load controls did not reproduce the product journey locally. A deterministic BSD adapter
characterization demonstrated a local mechanism consistent with the hosted symptom: nested
Expect/script PTYs could turn one line ending into `CRCRLF`, which normalizes to one lone carriage
return. Commits `054dea9`, `66f6d81`, and `29cc9c6` make the outer BSD Expect PTY raw/no-echo,
assign the single `opost onlcr` transform to the inner CLI PTY, split CI and TERM=dumb product
contracts, and retain a platform-scoped live termios regression. The strengthened test is RED on
the pre-fix macOS adapter, GREEN on the fixed BSD adapter, and GREEN on real Linux util-linux under
Node `24.15.0`. Fresh source and packed
Visual+ replays each passed 36/36 with retries disabled; focused tests passed 39/39, and build,
schemas, typecheck, zero-warning Biome, diff checks, artifact integrity, and CLI SHA-256 remained
unchanged. Independent final re-review reported C0/I0/M0. A new exact hosted `main` success is still
required before Task 3 can close or `v2.1.0` can be created.

Exact `main` run
[`29687703913`](https://github.com/vcode-sh/depfresh/actions/runs/29687703913) at
`b8a4f50532e58407a110e979231760e7fc7f66a7` passed macOS source Visual+ 36/36, Ubuntu source and
installed Visual+, full Test, Lint, Build, and Distribution Smoke. The macOS installed replay
failed with the retained `fallback-ci-pty` category; its later reduced-motion step was not reached.
That category identifies the CI fallback journey but not the violated assertion. The bounded private
report was cleaned as designed, so the historical assertion cannot be recovered. An exact
workflow-style local installed replay passed 36/36 under Node `24.15.0`, isolated npm `11.12.0`,
and hosted-like long paths, followed by 20/20 focused installed CI fallback controls. Commit
`0280309` therefore adds diagnostic evidence only: one shared CI journey is checked sequentially by
five exact title-only phases for execution/evidence, semantics, terminal controls, transition
uniqueness, and read-only repository state. Readiness chaining reports only the first violated phase;
the classifier still ignores failure messages, raw output, paths, and stacks. The retained verifier
now requires 40 tests. Fresh source and packed replays passed 40/40 without retries; focused
contracts passed 43/43, release gates passed 103/103, and build, schemas, typecheck, zero-warning
Biome, artifact identity, and diff checks passed. Independent review reported C0/I0/M0. This is
local diagnostic evidence only; a new exact hosted `main` run is required before any Task 3
checkbox or tag creation.

Exact `main` run
[`29688672949`](https://github.com/vcode-sh/depfresh/actions/runs/29688672949) at
`601c932012053b31da42a3611352bf39bb50c2aa` passed Ubuntu source and installed Visual+, full Test,
Lint, Build, and Distribution Smoke. The macOS source suite passed 39/40 tests, including all five
CI fallback diagnostic phases, then stopped in the first 40-column product journey with
`PTY process identity evidence changed`; installed replay and reduced-motion proof were therefore
not reached. The accompanying cleanup ambiguity was consequential to that primary failure. The
exact stack identified wrapper evidence registration. Control-flow analysis found a deterministic
mechanism consistent with the intermittent hosted result: a later snapshot can retain the same PID,
start token, and process group but overwrite the wrapper's original parent after a valid macOS
reparenting transition. The final sidecar comparison then contradicts the runner's own stable
identity definition. Private hosted evidence does not retain the compared identity tuples, so this
does not claim recovery of the exact transition from that run.

The local TDD correction preserves the first observed topology, still requires the sidecar parent
to match that first observation, restricts fixed `/bin/ps` inventory to the exact current numeric
user, and treats start/group mutation or absent-then-reappeared PID evidence as ambiguous without
adopting or signaling the changed identity. Deterministic RED produced three expected failures;
focused GREEN passed 6/6. Exact Node `24.15.0` source/verifier/readiness tests passed 66/66, source
Visual+ passed 46/46 without retries, and an isolated npm `11.12.0` packed replay passed 46/46 while
retaining the exact 56-file, 332890-byte artifact, SHA-512 integrity, and installed CLI SHA-256.
The complete no-retry suite passed 162 files and 2,177 tests; release gates passed 103/103;
schemas, typecheck, build, full zero-warning Biome, and diff checks passed. Two independent final
reviews reported C0/I0/M0, including direct Darwin and Linux procps same-user probes. This remains
local correction evidence pending a new exact hosted all-job success; no tag or publication
occurred.

Exact `main` run
[`29690064055`](https://github.com/vcode-sh/depfresh/actions/runs/29690064055) at
`c091c8d7fe87d35b9beeb1149ad0ccc56195cc20` passed Ubuntu source and installed Visual+, full Test,
Lint, Build, and Distribution Smoke. The macOS source suite passed 45/46 and again stopped in the
first 40-column success journey before installed replay and reduced-motion proof. The primary stack
now reached CLI evidence registration; cleanup ambiguity was again consequential. The retained
generic message combines parent, group, and start mismatches, so this run does not justify relaxing
any identity dimension. A diagnostic-only TDD slice now assigns fixed `wrapper`/`cli` roles and
allowlisted parent/group/start combination labels while preserving every comparison and ambiguity
effect. Its RED failed 6/6 against the generic implementation; exact Node `24.15.0` GREEN passed all
9 fixed-label/privacy cases and the unchanged Visual+ suite passed 46/46 without retries. The fixed
diagnostic message retains or reflects no PID, parent PID, process-group ID, start timestamp, path,
environment value, or raw child output value. This remains local diagnostic evidence; no release
gate is closed by it. The complete no-retry suite
passed 163 files and 2,186 tests; schemas, typecheck, full zero-warning Biome, and diff checks passed.
Two independent reviews reported C0/I0/M0 after one privacy-wording correction. A new exact hosted
run remains mandatory.

### Task 3: Hosted publication

**Files:**

- Commit: reviewed Plan 036 candidate files
- Create: annotated Git tag `v2.1.0` only after hosted main success

**Interfaces:**

- Consumes: exact local candidate.
- Produces: hosted trusted publication and curated GitHub release.

- [ ] **Step 1: Recheck publication preconditions**

Confirm exact staged scope, clean diff checks, version, npm/tag/release absence, local artifact
identity, and no unrelated staged files.

- [ ] **Step 2: Commit, push, and prove hosted main**

Push the English release commit and monitor the exact SHA. Expected: every required hosted source,
PTY, package, and portability job succeeds. Stop before tagging otherwise.

- [ ] **Step 3: Trigger immutable hosted release**

Recheck absence, create/push annotated `v2.1.0`, and monitor verify, publish, public-install, and
release jobs. Never move the tag or publish manually.

### Task 4: Public installed-product proof and closeout

**Files:**

- Modify: `docs/releases/v2.1.0.md`
- Modify: `plans/036-v2.1.0-release.md`
- Modify: `plans/README.md`
- Modify: `.superpowers/sdd/progress.md`

**Interfaces:**

- Consumes: hosted outputs and public npm/GitHub state.
- Produces: proven public `2.1.0` and closed Safe Write/Visual+ program.

- [ ] **Step 1: Verify npm identity and installed capabilities**

Require `latest=2.1.0`, matching integrity, isolated install, CLI version, capabilities, exports,
schemas, skill assets, and no unexpected runtime dependency.

- [ ] **Step 2: Replay public Visual+ journeys**

Run public installed bytes against the deterministic success/safety fixtures. Require all 76 rows,
14 targets, 18/39 shared surface, 2 major cards, correct receipts/exits, no terminal leak, and exact
final filesystem state.

- [ ] **Step 3: Verify GitHub bytes and provenance**

Require non-draft/non-prerelease release, byte-identical workflow/GitHub/npm tarballs, and SLSA
subject/digest binding to `refs/tags/v2.1.0`, release workflow, commit, run, and hosted builder.

- [ ] **Step 4: Close ledgers with observed evidence**

Record exact public identifiers, mark Plans 032-036 done, run documentation/static gates, and
commit/push only the reviewed closeout. Do not claim production completion before this step passes.
